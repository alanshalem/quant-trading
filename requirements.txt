# Runtime dependencies — kept in sync with [project.dependencies] in pyproject.toml.
# For development install the package with extras instead:
#     pip install -e ".[notebook,dev,docs]"

# Core data & numerical
polars>=1.0.0
numpy>=2.0.0
pandas>=2.2.0

# Machine learning
torch>=2.4.0
scikit-learn>=1.5.0

# Visualization
matplotlib>=3.9.0
seaborn>=0.13.0
altair>=5.4.0
vegafusion[embed]>=2.0.0
vl-convert-python>=1.8.0

# Data access
requests>=2.32.0
tqdm>=4.66.0

# Jupyter / notebook pipeline
ipykernel>=7.0.0
nbformat>=5.10.0
jupytext
nbconvert
